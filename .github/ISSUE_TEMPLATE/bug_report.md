---
name: Bug report
about: Report a bug in disk-clean
title: '[Bug] '
labels: bug
assignees: ''
---

**Environment**
- OS / build: (e.g. Windows 11 24H2)
- Version: `disk-clean --version`
- Mode: single EXE / Node.js

**Command run**
```
<command>
```

**Expected behavior**
...

**Actual behavior**
...

**Report data (paths only, no file contents)**
- Report JSON: paste the `summary` section from `~/.disk-clean/report.json` (paths OK; remove anything sensitive).
- Audit: last few lines of `~/.disk-clean/audit.jsonl` if relevant.

**Safety note**
Did the tool move/delete anything unexpectedly? (If yes, this is highest priority — include the `organize-map.json` if applicable.)
